# RMIT Connect Environment Configuration Template
# Copy this file to .env and update the values for your environment

# Application Configuration
SECRET_KEY=your-secret-key-here-minimum-32-characters
FLASK_ENV=development
BASE_URL=http://localhost:5001

# Database Configuration (Development)
# For development, SQLite is used automatically
# For production, set DATABASE_URL to your PostgreSQL connection string
# DATABASE_URL=postgresql://username:password@host:port/database_name

# Email Configuration (Optional for development)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# SMTP Configuration (Alternative email setup)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Server Configuration
HOST=0.0.0.0
PORT=5001

# Security Configuration (Advanced)
# These are optional and have sensible defaults
# SESSION_COOKIE_SECURE=True
# SESSION_COOKIE_HTTPONLY=True
# SESSION_COOKIE_SAMESITE=Lax

# Development vs Production Notes:
# - Development: Uses SQLite database automatically
# - Production: Requires DATABASE_URL to be set to PostgreSQL
# - Email: Optional for development, required for production user registration
